{
  "description": "Per-turn model routing: turn.start asks Jev which tier and effort, turn.step rewrites the request and writes a route line and one plain-language summary per reply, agent.spawn routes subagents, /jev reports and configures",
  "modules": [
    "./register.ts"
  ]
}
